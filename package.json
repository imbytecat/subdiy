{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "generate": "drizzle-kit generate",
    "migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "hono": "^4.5.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}